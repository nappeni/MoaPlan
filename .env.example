PORT=5173
APP_URL=http://localhost:5173
# Production requires PostgreSQL, a fixed 64-hex-character encryption key and setup token.
DATABASE_URL=
ENCRYPTION_KEY=
SETUP_TOKEN=
# NODE_ENV=production

# Supabase: path to the downloaded CA certificate (verify server TLS).
DATABASE_SSL_CA_FILE=
