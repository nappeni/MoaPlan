# 게시 동의 및 상태 조회 안내 — 2026-09-13

- 게시 요청에 동의 사용자·시각·검토 버전 저장. 진행/완료/결과 확인 상태는 동의 완료로 체크 고정. 기존 manual/jobId 기록에도 적용하며 새 초안이나 실패 후 재시도에는 새 동의 필요.
- 게시 상태 확인에 로딩 및 조회 결과 안내 추가. 서버에 저장된 상태 조회이며 완료 건의 Instagram 게시물 존재 여부를 별도로 조회하는 기능은 아님.
- 동의 기록 테스트 추가, 전체 28개 통과·빌드 성공. 실제 게시하지 않음.

---

# 게시용 이미지 저장 흐름 개선 — 2026-09-13

- 게시용 이미지 생성·저장 완료 후 이미지 ID와 현재 초안을 함께 저장. 별도 초안 저장을 다시 눌러야 게시 가능했던 혼동 제거. 실패 시 자동 게시 없음, 검토 동의 유지 필수.
- 게시 버튼과 안내가 같은 조건을 사용하도록 정리: 이미지 미생성, 미저장 변경, 기획 미확정, 연동 설정, 검토 동의 및 게시 상태별 사유 표시.
- 빌드와 기존 테스트 28개 통과. 실제 Instagram 게시 및 R2 쓰기 검증은 실행하지 않음.

---

# 배경 원본 색감 보존 — 2026-09-13

- 공통 홍보 렌더러의 강제 녹색 오버레이 제거. 배경 밝기를 샘플링해 밝은 배경은 어두운 글자, 어두운 배경은 밝은 글자와 얇은 외곽선 적용. 단체/활동 미리보기·다운로드·게시용 이미지 공통 반영.
- 기존 AI 배경 재생성 불필요. 저장된 게시용 JPEG는 이미지 생성 후 초안을 저장해야 변경됨.

---

# 홍보 표지 우선 구성 — 2026-09-13

- 단체/활동 홍보 첫 장을 cover로 저장하고 표지 전용 제목 배치 적용. 편집 첫 탭은 01 표지, 첫 장 이동/삭제 금지. 생성·다운로드·게시용 이미지 순서는 표지부터.
- AI 구성안에 첫 장은 짧은 표지 제목과 소개, 두 번째 장부터 상세 설명을 작성하도록 지정. 표지 포함 최대 8장.
- 저장된 기존 초안은 본문을 유지하고 앞에 표지 추가. 이미 8장이면 9장을 편집 상태로 보존하고 8장 이하로 정리 후 저장하도록 안내. 게시 완료/처리 중/예약/결과 확인 상태의 기존 자료는 변경하지 않음.
- 표지 추가 뒤 게시용 이미지를 다시 생성하고 홍보 초안 저장 필요. 로고가 있으면 로고만, 없으면 단체명 표시 유지.

---

# 입력 내용 별도 저장 및 AI 오류 안내 — 2026-09-13

- 첫 화면과 목적·컨셉 편집 화면에 입력 내용 저장 버튼 추가. AI 호출 없이 DB 저장. 기존 홍보물 문구/이미지는 유지하며 목적 변경 시 stale로 표시해 재검토 전 게시 차단.
- HTTP 400 보고 대응: Responses JSON 모드의 실제 input에도 JSON 출력 지시 명시. 과거 원격 오류 원문을 보관하지 않아 정확한 원인은 미확정. 인증/모델/출력 형식/잔액/요청 한도별 고정 오류 안내 추가(원격 원문/키 비노출).
- 별도 입력 저장, 버전 충돌, JSON 요청 형식과 오류 원문 비노출 테스트 포함 전체 26개 통과. 실제 유료 AI 호출 및 Instagram 게시는 실행하지 않음.

---

# 목적·컨셉으로 AI 초안 생성과 재생성 — 2026-09-13

- 첫 작성 화면의 AI 홍보 초안 만들기는 입력한 목적·컨셉으로 AI 구성안(이미지 문구, 캡션, 해시태그)을 생성한 후 편집 화면을 연다. 배경 일러스트 실제 생성은 별도 버튼 유지.
- 편집 화면에서 제목·목적·컨셉 수정 후 AI 초안 다시 만들기 가능. 생성 결과는 화면상 검토본이며 홍보 초안 저장을 눌러야 기존 저장본 및 입력 목적·컨셉이 함께 갱신된다.
- 생성 실패 시 입력과 기존 저장본 유지. 저장 시 버전을 확인하여 다른 화면의 오래된 저장을 차단. AI 작업 중 첫 입력 화면의 변경/목록 이동 비활성.
- 모의 AI로 생성·재생성·실패 시 기존 저장본 보존·명시 저장·오래된 저장 충돌 검증. 전체 테스트 25개 통과, 빌드 성공. 유료 AI/Instagram 실제 게시 테스트는 실행하지 않음.

---

# 단체 홍보와 관리자 수동 게시 — 2026-09-13

- Cloudflare Pages / Supabase Edge에 단체 홍보 메뉴 배포 완료. 단체별 `promotions` 컬렉션에 별도 저장하며 활동/캘린더에 섞이지 않음. 데이터 내보내기에 포함.
- 단체 소개와 홍보 목적 입력 → AI 구성안/배경 또는 직접 편집 → 압축 이미지 생성·저장 → 초안 저장 → 저장된 이미지와 캡션 검토 → 명시적 동의 후 Instagram 게시.
- 활동 홍보도 동일한 저장 피드 미리보기와 수동 게시 사용. Instagram ID/토큰/API 버전 미등록 시 게시 비활성. 서버에서도 설정·최신 검토 시각·동의를 확인.
- 수동 게시 요청만 `manual:true`로 기록. 브라우저가 해당 단체/홍보물/jobId의 publish-step을 반복 호출하며 작업 상태는 DB에 보존. 화면을 닫으면 다시 해당 홍보물을 열어 이어서 확인해야 함. 통신 오류는 게시 상태 확인 버튼으로 재개. 게시 요청 응답 유실은 attention으로 전환하여 자동 재게시 금지.
- 기존 자동 예약 Cron과 Edge worker는 계속 비활성. 수동 작업은 일반 예약 worker의 처리 대상에서 제외.
- 게시 완료/실패/권한 오류/요청 한도/불명확한 결과를 화면에 표시. 원격 오류 원문이나 비밀값은 표시하지 않음.
- 테스트 24개 통과: 단체 격리, 활동과 분리, 검토 동의와 최신성, 설정 누락, 선택한 작업만 실행, 중복 게시 방지, 단일/다중 이미지 단계 처리 및 실패/중단 복구 포함.
- 운영 브라우저에서 새 메뉴·작성 화면 확인. 격리된 로컬 데이터로 초안 저장, 새로고침 복원, 설정 없는 게시 비활성, 이미지 미리보기 확인.
- 실제 Instagram 게시/유료 AI 생성/R2 쓰기 테스트는 실행하지 않음(기존 사용자 허용 범위: 조회·연결 검증).
- 직전 UI 수정(대표 색상 미리보기, 화면 새로고침 복원, 후보지 작성 중 등록 버튼 숨김, 목록 버튼 문구)도 현재 배포에 포함.
- 이전 문서의 Git 푸시 차단 기록은 과거 상태. d92f4b0까지 원격 반영 완료했고 이번 변경 사항은 아직 미커밋 상태.

---

# 예약 기반 설치 결과 — 2026-09-13

- 사용자 명시 승인 후 scripts/install-scheduler.mjs 실행 완료.
- pg_cron/pg_net 설치, 동일 프로젝트 Vault에 프록시 비밀값 보관, 비공개 trigger_publishing_step 함수 및 10초 간격 Cron 등록 완료.
- Cron active=false, Edge 예약 엔드포인트 503(비활성) 확인. anon/authenticated 역할의 함수 EXECUTE 권한 없음 확인.
- 실제 게시·일정 등록은 실행하지 않음.
- 개발 코드 커밋 f631b52는 로컬에 보존. GitHub 푸시는 로컬 인증 gomgom-code의 저장소 권한 부족(403), 연결된 nappeni GitHub 앱의 쓰기 권한 부족(403)으로 미완료. nappeni의 Git 쓰기 인증을 연결한 후 일반 push 필요. 전역 Git 인증은 변경하지 않음.

---

# 추가 개발 및 검증 — 2026-09-12

- Pages: https://moaplan.pages.dev, 기존 계정의 로그인 세션 및 관리 화면 확인.
- SVG 파비콘 추가. 브라우저 업로드 전 1080x1350 이내 축소, 서버 JPEG 180KB 목표/300KB 상한, WebP 썸네일 160px. 원본은 저장하지 않음. 작은 이미지는 확대하지 않으며 투명 배경은 흰색 처리.
- Edge 이미지 처리 복구 완료: npm 패키지 내부 WASM을 직접 읽음. 별도 공개 WASM 다운로드/리소스는 불필요하여 제거. 합성 이미지의 실제 Edge 압축 성공 확인.
- 로그인 요청 제한을 DB 트랜잭션 기반 공유 카운터로 교체.
- 예약 작업을 단계별 실행, DB 임대 잠금, 컨테이너 진행 상태 저장 구조로 변경. 게시 요청 후 응답 유실/중단 시 attention 처리하고 자동 재게시하지 않음.
- 예약 실행은 여전히 비활성. 활성화 전에는 게시 요청을 503으로 명확히 거절.
- scripts/install-scheduler.mjs는 설치 준비만 완료. 운영 DB 확장/Vault/함수/Cron 변경은 자동 승인 검토에 차단되어 아직 실행하지 않음. 명시 승인 후 실행해도 Cron은 중지 상태로 생성됨.
- 실제 Edge에서 기존 DB 비밀 복호화 및 R2/Google/OpenAI 읽기 연결 성공. Instagram 계정 정보 미등록.
- 사용자는 외부 서비스 검증을 조회/연결까지만 허용. AI 실제 생성, R2 쓰기, Google 일정 등록, Instagram 실제 게시 테스트는 실행하지 않음.
- Google 기존 refresh token 연결은 정상. 새 배포 주소의 OAuth 재동의 및 redirect URI 설정 검증은 별도 필요.
- 테스트 22개 통과. 이미지 용량/투명도/소형 이미지, 공유 제한, 동시 예약 처리, 다중 이미지, 중단 복구, 응답 유실 후 중복 방지 검증. 브라우저 새로고침 후 로그인 세션 유지 확인.

---

# 노트북 배포 진행 — 2026-09-12

- 최초 테스트 배포 완료: https://moaplan.pages.dev
- Supabase MoaPlan 프로젝트의 moaplan-api Edge Function을 --use-api 방식으로 배포. 로컬 Deno 검사와 Docker는 실행하지 않음.
- 기존 DB URL, CA, 암호화 키는 사용자 명시 승인 후 Supabase 비밀 설정에 등록. Pages에는 API URL 및 프록시 비밀값만 등록.
- Edge의 Buffer 전역 미정의 오류를 node:buffer 명시 import로 수정.
- 배포 검증: 화면 200, Pages API health 200, session에서 원격 DB 및 기존 계정 존재 확인, 미로그인 data 401, 다른 출처 POST 403, 프록시 비밀값 없는 Edge 직접 접근 403.
- 테스트 13개 통과, Vite 및 Pages 함수 빌드 성공. 실제 계정 로그인 및 로그인 이후 외부 연동은 아직 미검증.
- 예약 자동 실행은 비활성. Node는 MOAPLAN_WORKER_ENABLED=true일 때만 실행. Edge에는 예약 트리거 없음.
- 첫 Edge 배포의 이미지 업로드는 503 안내 오류로 제한. 로컬 Node 이미지 처리는 유지. WASM 배포 전략/리소스 검증 필요.
- 기존 로그인 제한은 인스턴스별 메모리에 있으므로 공유 제한 처리가 남음. Google OAuth 리디렉션 주소 설정 및 실연동 검증도 남음.
- npm run dev:local은 .local/development의 격리 데이터 사용. npm run dev는 복원한 .env의 실제 Supabase 데이터 사용.
- 배포 설정 등록 스크립트: scripts/configure-deployment.mjs. 생성된 설정 비밀값은 Git 제외 .local/deploy에 소유자 전용 권한으로 보관. 기존 .env 및 암호화 키는 변경하지 않음.
- 현재 배포는 로컬 변경 사항을 직접 업로드한 상태. Git 커밋/푸시는 아직 하지 않음.

---

# 작업 인계 — 2026-09-11

## 현재 상태
- Supabase PostgreSQL 연결 및 기존 로컬 데이터 이전 완료. 저장 위치는 moaplan_private.moaplan_state.
- 설정 저장 시 메타데이터로 요청이 거부되던 문제 수정.
- R2 업로드/서명 URL 다운로드 검증 완료, 테스트 파일 삭제 완료.
- OpenAI 인증 및 모델 목록 조회 확인. 실제 생성은 미검증.
- Google OAuth 연결 및 캘린더 목록 조회 확인. 일정 등록은 미검증.
- Instagram 연결 및 실제 게시 미완료.

## 서버 전환 진행 중 (배포 완료 아님)
- server/app.js: Express API를 로컬 실행 진입점에서 분리.
- server/images-node.js / images-wasm.js: 이미지 처리 분리 및 WASM JPEG/WebP 기본 출력 확인.
- functions/api/[[path]].js: Cloudflare Pages API 프록시 초안.
- supabase/functions/moaplan-api/index.ts 및 설정 파일: Edge Functions 진입점 초안.
- Node 테스트 7개는 앱/이미지 분리 후 통과. 마지막 Edge 진입점 변경 이후 전체 검증 미완료.
- 예약 게시를 짧고 재개 가능한 작업으로 변경하는 작업, WASM 배포 번들/리소스 제한 확인, 서버리스 로그인 요청 제한, 프록시/쿠키/OAuth 통합 검증이 남음.
- 배포 도구(Supabase CLI, Wrangler)는 로그인하지 않은 상태였음.
- Render 설정은 이전 구성이다. 현재 목표는 Cloudflare Pages + Supabase Edge Functions/PostgreSQL + R2.

## PC 중단 현상
Deno 호환성 검사 시도 도중 사용자가 블루스크린과 PC 멈춤을 보고했다. 원인은 확인되지 않았다. 동일 PC에서 Deno 검사를 재시도하지 않는다. 이번 커밋/푸시에서는 빌드나 런타임 검사를 실행하지 않았다.

## 노트북에서 이어갈 때
1. 저장소를 복제하고 이 문서부터 확인한다.
2. .env, .local/encryption.key 및 인증서 등은 Git에 없다. 기존 PC에서 안전하게 별도 전달한다.
3. ENCRYPTION_KEY는 기존 값 그대로 사용한다. 새 키로 바꾸면 DB의 기존 암호화 설정을 복호화할 수 없다.
4. .env의 DATABASE_SSL_CA_FILE 경로가 새 PC에서 유효한지 확인한다.
5. 실제 데이터는 Supabase에 있다. 오래된 .local/state.json을 다시 업로드해 덮어쓰지 않는다.
6. 문제 원인이 확인되기 전에는 중단된 Deno 명령을 자동 재실행하지 않는다.

비밀번호/API 토큰/환경 변수 원문은 문서·커밋·로그에 포함하지 않는다. docs/MoaPlan_env.txt는 로컬 비밀 정보 참고 파일로 Git 제외 대상이다.
